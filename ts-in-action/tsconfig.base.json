{
    "files": [ // 编译器需要编译单个文件的数组
        "src/c.ts"
    ],
    "include": [ // 编译器需要编译的文件和目录
        // "src"       // 包括src下面所有的文件
        // "src/*"     // 只编译src根目录下面的文件
        "src/*/*" // 只编译src下面的二级文件
    ],
    "exclude": [ // 排除编译器排除的文件

    ]
}